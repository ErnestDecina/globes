import * as dotenv from 'dotenv';
dotenv.config();

export const application_name: string = "Globes API";
export const server: string = "Globes API";
export const is_development: boolean = true
                                        