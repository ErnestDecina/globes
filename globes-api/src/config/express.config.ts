import * as dotenv from 'dotenv';
dotenv.config();

export const express_port: number = 3000;
export const api_version: string = 'v1';
export const application_secret: string = "TODO: REPLACE THIS WITH A ENV VAR IMPORT";